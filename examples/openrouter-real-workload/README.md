# Real OpenRouter catalog x real workload shape (example)

Metered-API frontier across OpenRouter's catalog (live prices pulled from the
public catalog API) priced at a real 30-day agent token shape
(~15k uncached / ~167k cache-read / ~600 output tokens per successful turn).
Quality axis: Artificial Analysis Intelligence Index.

Models without a verified AA score are excluded with explicit reasons in
provenance (deepseek-v4-pro-0813, kimi-k3, minimax-m3) — never silently
dropped.
